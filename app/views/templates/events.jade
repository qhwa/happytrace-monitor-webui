h1.ui.header events
table.ui.sortable.table.segment#events
    thead
      tr
        th.time time
        th.user user
        th.action action
    tbody
      tr( ng-repeat="evt in events" )
        td {{evt.time | amDateFormat:'L dddd h:mm:ss a'}}
        td {{evt.user}}
        td {{evt.action}}
